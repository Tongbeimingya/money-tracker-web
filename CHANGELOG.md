# 更新日志

## 2026-09-01 - 周期管理与多维度统计

### 新增功能

#### 1. 周期管理系统
- **灵活的周期类型**
  - 支持月度周期：自动按自然月管理
  - 支持自定义周期：用户自定义开始时间和名称
  - 周期状态管理：进行中/已结束

- **周期管理界面** (设置页面)
  - 显示当前周期信息（类型、名称、开始时间、运行天数）
  - 开始新周期：选择类型、设置名称、开始时间、预算目标
  - 结束当前周期：归档当前周期数据
  - 历史周期列表：查看和切换到历史周期
  - 快速操作：立即开始按钮

#### 2. 多维度图表统计
- **时间维度切换**
  - 日视图：按每日统计支出
  - 周视图：按每周统计支出
  - 周期视图：按当前记账周期统计（自动适配周期长度）
  - 月视图：显示最近6个月的支出趋势

- **智能图表显示**
  - 周期小于15天：按日显示
  - 周期大于15天：按周显示
  - 自动计算ISO周数
  - 动态调整图表标题

#### 3. 数据模型重构
- **从月度预算迁移到周期系统**
  - periods数组：存储所有记账周期
  - currentPeriodId：追踪当前活跃周期
  - 自动数据迁移：将旧的monthlyBudgets转换为periods
  - 保持向后兼容：确保现有数据不丢失

- **周期管理API**
  - createPeriod：创建新周期
  - endCurrentPeriod：结束当前周期并归档
  - switchPeriod：切换查看历史周期
  - 所有分类和支出自动关联到周期

### 界面优化

#### 统计页面
- 添加时间范围切换器（圆角按钮组）
- 优化图表布局和标题显示
- 保留原有的分类占比环形图和分类支出统计

#### 设置页面
- 全新的周期管理卡片
- 周期状态徽章（月度/自定义）
- 历史周期列表（可点击切换）
- 改进的表单对话框（新建/结束周期）
- 快速操作按钮

### 技术改进
- 异步数据加载：所有页面onMounted改为async/await
- 数据持久化：localStorage自动保存
- 响应式设计：所有新组件适配移动端
- 类型安全：完整的数据结构定义

### Bug修复
- 修复了分类点击导航问题（使用事件通信代替路由）
- 修复了数据不显示问题（异步加载时序）
- 修复了预算进度条显示问题（移除冗余字段）
- 修复了小分类布局问题（改为垂直列表）

### 数据结构

```javascript
// 新的周期数据结构
{
  periods: [{
    id: string,
    type: 'month' | 'custom',
    name: string,
    startDate: timestamp,
    endDate: timestamp | null,
    status: 'active' | 'completed',
    totalTarget: number,
    totalReceived: number,
    createdAt: timestamp
  }],
  currentPeriodId: string,
  categoryGroups: [...],
  categories: [...],
  expenseRecords: [...],
  incomeRecords: [...]
}
```

### 使用说明

1. **开始新周期**
   - 进入设置页面
   - 点击"开始新周期"
   - 选择月度或自定义类型
   - 设置名称和开始时间
   - 可选设置预算目标

2. **查看多维度统计**
   - 进入统计页面
   - 使用图表上方的切换按钮
   - 选择日/周/周期/月查看不同时间维度的支出趋势

3. **管理历史周期**
   - 设置页面可查看所有历史周期
   - 点击历史周期可切换查看
   - 切换后所有数据显示对应周期的内容

### 下一步计划
- 周期总结报告生成
- 周期对比功能
- 导出周期数据为PDF/Excel
- 预算预警和提醒功能
